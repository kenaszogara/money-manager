import React from 'react'

function AddPage() {
  return (
    <div>
      {/* Categories Icon */}
      <div style={{
        width: '100%',
        height: '319px',
        background: '#FFFFFF',
        borderRadius: '5px',
        marginBottom: '1rem',
      }}>
        Categories Box
      </div>

      {/* Input */}
      <div style={{
        width: '100%',
        height: '132px',
        background: '#FFFFFF',
        borderRadius: '5px',
        marginBottom: '1rem',
      }}>
        Input Box
      </div>

    </div>
  )
}

export default AddPage